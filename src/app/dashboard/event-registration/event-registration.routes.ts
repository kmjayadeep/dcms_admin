import { Route } from '@angular/router';

import { EventRegistrationComponent } from './index';

export const EventRegistrationRoutes: Route[] = [
	{
		path: 'eventRegistration',
		component: EventRegistrationComponent
	}
];
