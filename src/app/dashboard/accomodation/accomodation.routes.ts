import { Route } from '@angular/router';

import { AccomodationComponent } from './index';

export const AccomodationRoutes: Route[] = [
	{
		path: 'accomodation',
		component: AccomodationComponent
	}
];
